# Sao chép file này thành `.env.local` rồi điền key thật (KHÔNG commit .env.local).
# Lấy key tại https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Tuỳ chọn — đổi model nếu cần (mặc định: gpt-4.1-mini)
# OPENAI_MODEL=gpt-4.1-mini
